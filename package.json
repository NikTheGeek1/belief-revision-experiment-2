{
  "name": "moral-belief-updating-version-2",
  "version": "1.0.0",
  "description": "back-end",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm start server.js",
    "start-server": "node server.js",
    "heroku-postbuild": "cd online-app && npm install && set INLINE_RUNTIME_CHUNK=false && npm run build"
  },
  "author": "nikos theodoropoulos",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "helmet": "^4.1.0"
  }
}
